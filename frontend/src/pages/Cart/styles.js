/* import { styled } from "@mui/material";
import { Card } from "@mui/material";

export const CheckoutCard = styled(Card)(({ theme }) => ({
  [theme.breakpoints.down("md")]: {
    position: "fixed",
    width: "100%",
    bottom: 0,
    left: 0,
  },
})); */
